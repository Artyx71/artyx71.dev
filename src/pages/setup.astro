---
import BaseLayout from '@/layouts/BaseLayout.astro'

const sections = [
  {
    label: 'editor',
    lines: ['Neovim with lazy.nvim. LSP-first config, no IDE features.'],
    link: { label: 'dotfiles', href: 'https://github.com/Artyx71/dotfiles' },
  },
  {
    label: 'window manager',
    lines: ['Hyprland. Tiling, minimal gaps, no decorations.'],
    link: { label: 'hyprland config', href: 'https://github.com/Artyx71/dotfiles' },
  },
  {
    label: 'terminal',
    lines: ['WezTerm. GPU-accelerated, lua config.'],
    link: { label: 'wezterm config', href: 'https://github.com/Artyx71/dotfiles' },
  },
  {
    label: 'shell',
    lines: ['zsh + starship prompt.'],
    link: { label: '.zshrc', href: 'https://github.com/Artyx71/dotfiles' },
  },
  {
    label: 'multiplexer',
    lines: ['tmux. session management for long-running dev tasks.'],
    link: { label: 'tmux.conf', href: 'https://github.com/Artyx71/dotfiles' },
  },
]
---

<BaseLayout
  title="Setup"
  description="Development environment — editor, window manager, terminal, shell, and multiplexer."
>
  <section class="py-12">
    <p class="font-mono text-xs text-text3 uppercase tracking-widest mb-8">setup/</p>

    <div class="space-y-8">
      {sections.map(({ label, lines, link }) => (
        <div>
          <p class="font-mono text-xs text-text3 uppercase tracking-widest mb-2">{label}</p>
          {lines.map((line) => (
            <p class="text-sm text-text2 mb-1">{line}</p>
          ))}
          <a
            href={link.href}
            target="_blank"
            rel="noopener noreferrer"
            class="font-mono text-xs text-text2 hover:text-accent transition-colors"
          >
            → {link.label}&#x2197;
          </a>
        </div>
      ))}
    </div>
  </section>
</BaseLayout>
