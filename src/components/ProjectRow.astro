---
interface Props {
  title: string
  tagline: string
  status: 'active' | 'WIP' | 'archived'
  href: string
}

const { title, tagline, status, href } = Astro.props
---

<a
  href={href}
  class="flex flex-col gap-1 py-3 border-t border-border group transition-colors hover:border-accent sm:grid sm:grid-cols-[12rem_1fr_5rem] sm:gap-4 sm:items-baseline"
>
  <span class="font-mono text-sm text-text1 group-hover:text-accent transition-colors">
    {title}
  </span>
  <span class="text-sm text-text2">
    {tagline}
  </span>
  <span class="font-mono text-xs text-text2 sm:text-right">
    {status}
  </span>
</a>
